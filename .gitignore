# ╔═══════════════════════════════════════════════════════════════════╗
# ║ ОБЩАЯ ШАПКА — одинакова во всех репозиториях контура.             ║
# ║ Правится сразу везде, иначе расходится молча: до 2026-08-14       ║
# ║ байт-код Python ловили три репозитория из пяти, ключи — один,     ║
# ║ и это был публичный. Хвост ниже — своё у каждого.                 ║
# ╚═══════════════════════════════════════════════════════════════════╝

# Локальные настройки Claude Code (разрешения конкретной машины)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Windows
Thumbs.db
desktop.ini

# Учётные данные и ключи. Стоят и в приватных репозиториях:
# приватный — не «можно», а «дешевле последствия».
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
.env
.env.*
!.env.example
credentials.*
*_rsa
*_ed25519
*.dump
*.sql

# Отложенные версии и артефакты прогонов — живут в X:\_archive,
# вне репозиториев
_archive/

# ─── дальше своё ────────────────────────────────────────────────────


# ---------------------------------------------------------------------
# Не публикуется в этом репозитории.
# architecture-ways — ТОЛЬКО архитектурные подходы: детализация,
# кейсы применения, антипримеры, референсы на публичные проекты.
# Остальное живёт в других репозиториях либо только локально.
# ---------------------------------------------------------------------

# Приватный продукт — целевой процесс. Не для публики.
PIPELINE.md

# Внутренние исследования — не для публики, только локально.
REVIEW.md

# Презентации → репозиторий temporary-knowledge
PIPELINE-PRESENTATION.md
SDD-PRESENTATION.md

# Разбор конкурента → temporary-knowledge (содержит внутренние выводы)
openspec-analysis.md

# Рабочие заметки по фабрике → skills-factory
skill-factory-model-selection.md

# Конфиденциальные материалы — не должны попасть в публичный репозиторий.
# Дублирует .aiignore; там — про передачу ИИ, здесь — про публикацию.
private/
confidential/
internal/
secrets/
dumps/
