# architecture-ways — правила репозитория

**Репозиторий публичный.** Всё, что сюда попадает, видно снаружи.

## Что здесь

| Файл | Что это |
| --- | --- |
| `ARCHITECTURE.md` | каталог приёмов проектирования систем со слабыми моделями — 94 штуки в девяти группах |
| `map/dependencies.yaml` | карта зависимостей между приёмами, машиночитаемая |
| `analysis/` | разборы чужих решений: что взяли, что отвергли |
| `measurements/` | собственные замеры |

## Правила публикации

**Не попадают в этот репозиторий:**

- имена модулей, сервисов, топиков и пакетов рабочего продукта;
- имя платформы и организации, коды ошибок, маркерные константы
  контракта между подсистемами;
- адреса: внутренние хосты, эндпоинты, сетевые пути;
- ключи задач трекера;
- указатели на авторов разбираемых проектов, их принадлежность
  и время работы — разбор про приёмы, а не про людей.

**Перечня самих строк здесь нет, и это намеренно.** Прежняя редакция
правила называла запрещённые имена поимённо — и тем публиковала их:
список запрещённого сам является тем, что запрещено. Конкретные строки
живут в `~/.claude/projects/forbidden-names.txt`, вне всех
репозиториев, и проверяются гейтом `tools/check_names.py`.

Материал с настоящими именами живёт в приватных репозиториях.
**Публичная версия делается вычиткой из приватной, не наоборот.**

Имена из чужих открытых проектов (`TOOL_NAME_RE`, `CLAUDE_PROJECT_DIR`)
скрывать незачем — они предмет разбора.

Проверка перед коммитом:

```bash
python tools/check_names.py --all
```

## Пуш

Коммиты делаются здесь, **пуш выполняет владелец**. «As is» означает:
не редактировать содержимое ради улучшения — замечания высказывать
текстом, а не правкой.

## Правило пополнения каталога

**Правило относится к оценке, а не к записи** — уточнено 2026-08-03.

Прежняя формулировка звучала «приём записывается только после
применения у себя» и противоречила самому каталогу: большинство
приёмов взято из разбора чужих проектов и общей практики, и все они
записаны. Правило в таком виде запрещало и работу, ради которой
каталог ведётся, — разбор чужих решений.

Действующий порядок:

| Оценка | Что означает |
| --- | --- |
| **берём** | применено у себя и работает |
| **кандидат** | идея из чужого проекта или из общих соображений, у себя не применялась |
| **позже** · **осторожно** · **не берём** | как раньше |

Записать идею можно сразу — но **со статусом `кандидат`**. Статус
`берём` даётся только после применения, и подтверждается артефактом:
работающим скриптом, прогоном или разобранным инцидентом, а не
памятью о том, что «вроде делали».

**Переоценка проведена 2026-08-03:** было 53 «берём», стало 35,
восемнадцать переведены в кандидаты. Критерий механический — приём
засчитан применённым, если есть **и скрипт, и след его работы**:
файл результата, отчёт или тест. «В коде есть функция» не считается.

**Пополнение 2026-08-03:** семь приёмов из разбора чужих решений —
§2.9, §2.10, §6.8, §7.5 (инструменты SDD) и §5.8, §8.5, §8.6 (движок
векторного поиска), все «кандидат».

**Пополнение 2026-08-07:** три приёма из разбора набора скиллов
разработки — §2.11 (замысел и план — разные артефакты), §7.6 (контракт
с соседями внутри единицы работы), §7.7 (крупный аргумент передаётся
файлом), все «кандидат». Разбор — `analysis/superpowers.md`.

**Пополнение 2026-08-08:** §6.9 — программировать против общего среза
контрактов, из разбора `multi-repo-project-hooks`. Единственный приём,
взятый сразу со статусом **«берём»**: подтверждён собственным замером
(`format: json` соблюдён одной облачной моделью и проигнорирован другой),
и у него есть и код, и след работы.

**Пополнение 2026-08-14:** девять приёмов из разбора движка графа
знаний и фреймворка SDD (`analysis/open-gsd.md`) — §2.12, §2.13, §3.8,
§4.9, §5.9, §7.8, §9.7, §9.8, §9.9, все «кандидат». Шесть из девяти
несут наблюдённый отказ источника; у §5.9 отказ и собственный —
стена из 44 вердиктов после переписывания документа. Карта сведена
с каталогом 2026-08-21: неделю провенанс указывал в прошлое.

**Пополнение 2026-08-21:** пять приёмов из разбора четырёх обвязок
агентного CLI (`analysis/opencode-harnesses.md`) — §2.14, §6.10, §7.9
(«берём»: есть скрипт и след), §6.11, §6.12 («кандидат»). §6.10 —
приём с наблюдённым отказом: ограды, писанные по представлению
о формате, не сработали ни разу. Плюс три уточнения без новых номеров
(§7.8, §5.5, §9.7) и строка в §10 (переходящие «уроки» прозой).

**Пополнение 2026-08-28:** три приёма из разбора рантайма устойчивого
исполнения (`analysis/langgraph.md`) — §2.15 (предел на молчание
отдельно от предела на длительность), §2.16 (класс отказа решает,
повторять ли), §7.10 (частичный зачёт незавершённого такта), все
«кандидат». Повод разбора — вопрос о внедрении рантайма в конвейер;
вердикт «не зависимость, а материал» записан в разборе. Плюс два
уточнения без новых номеров (§2.10 — третий источник, §5.9 — второй)
и строка в §10 (пауза внутри единицы работы).

**Пополнение 2026-09-09:** пять приёмов из разбора панельной проверки
требований (`analysis/reqcheck.md`) — §4.10 («не читал» и «прочитал —
чисто» — разные исходы), §5.10 (отсев отключается при неполном
составе источников), §5.11 (вопросы человеку дозируются по графу
предпосылок), §6.13 (второе мнение — только другая модель), §9.10
(чистый эталон меряет выдумку), все «кандидат». §5.11 несёт
собственный наблюдённый отказ — ту же стену из 44 вердиктов, что
у §5.9: приём закрывает её вторую половину. Повод разбора — что взять
для конвейера постановок; вердикт «целиком не брать, материал брать»
записан в разборе, практическая часть ушла в планы адаптации
OpenSpec. Плюс два уточнения без новых номеров (§2.13 — второй
источник, §3.8 — второй) и строка в §10 (числовая уверенность поверх
самооценки модели).

**Пополнение 2026-09-09, второе:** пять приёмов из разбора рабочей
обвязки управления требованиями (`analysis/req-harness.md`; источник
обезличен — рабочий продукт, в карте значится как `req-harness`) —
§2.17 (оценщик считает балл, решатель цикла видит только балл),
§3.9 (срок годности знания — по классу записи, с источником
пересборки), §4.11 (сделанность проверяется следом в файловой
системе, повтор — диффом против снимка «до»), §7.11 (задание
субагенту ограничено числом, выведенным из отказа), §9.11 (механизм
обвязки сносится по измеренному неиспользованию), все «кандидат».
Четыре из пяти несут наблюдённый отказ источника — в счётчик
собственных отказов они не входят. Повод разбора — первый источник,
прожитый в эксплуатации на моделях класса собственного контура.
Плюс три уточнения без новых номеров (§3.7 — второй источник:
гейт называет инцидент происхождения и несёт воспроизводящий его
регрессионный тест; §4.10 — второй источник: явный вердикт «проверка
не проводилась»; §5.11 — второй источник: дозирование без графа,
порция совпала независимо) и две строки в §10 (взвешенный балл
из текстовых причин; гейт, вызываемый дисциплиной исполнителя).

Итого 94: 39 «берём», 52 «кандидат», 2 «позже», 1 «осторожно».
Счётчики сверяются тестами
`substrate-building/tests/test_catalog.py` и `test_docs.py`.

**Проверять ответ облака грепом по тексту, а не по номерам строк.**
Из 18 цитат разбора дословны 18, из 18 номеров строк верен один —
отклонения до 417 строк. Модель держит содержимое поданного файла
и не держит его позицию.

Три приёма применены частично и перечислены в `partially_applied`
карты — чтобы «берём» у них не читалось как полное применение.

У приёма обязательны: область, «зачем», отличие от соседних приёмов,
цена и **граница применимости**. Приём с наблюдённым отказом ценнее
приёма из общих соображений: таких приёмов одиннадцать и самих
отказов одиннадцать — на §6.7 приходится два отказа, а стена
вердиктов (§5.9 и §5.11) — один отказ на два приёма.

## Пометка источника

В каталоге три пометки: **Other** — из разбора другого проекта,
**SELF** — собственная наработка, **PRACT** — общая практика.

Буквенных кодов на каждый проект (`GB`, `SD`, `OS`) больше нет:
разобранных проектов становится больше, и код перестаёт читаться
раньше, чем перестаёт помещаться. Конкретный проект указывается
в `map/dependencies.yaml` полем `source_projects`.

## Язык каталога

**Второго лица нет.** Не «вы рассмотрели», а «было рассмотрено»; не
«ваш случай», а «тот же случай». Каталог — справочник, полезный любому
читателю, а не письмо конкретному человеку. Проверка простая: поиск
по `вы`, `ваш`, `вам`, `вас` должен давать ноль.

Изменение состава приёмов требует правки `map/dependencies.yaml`:
счётчик, группа, связи. В карте есть поле с хешем источника — при
коммите оно обновляется, иначе провенанс указывает в прошлое.
